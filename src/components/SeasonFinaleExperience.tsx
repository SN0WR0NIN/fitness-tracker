'use client';

import Link from 'next/link';
import { Crown, Download, Medal, Share2, Sparkles, Trophy, Users } from 'lucide-react';
import { useState } from 'react';
import type { SeasonFinale } from '@/lib/season-finale';

export default function SeasonFinaleExperience({data}:{data:SeasonFinale}){
  const [message,setMessage]=useState('');
  const buildImage=async()=>{const canvas=document.createElement('canvas');canvas.width=1080;canvas.height=1350;const ctx=canvas.getContext('2d');if(!ctx)throw new Error('Canvas unavailable');const bg=ctx.createLinearGradient(0,0,1080,1350);bg.addColorStop(0,'#06101e');bg.addColorStop(.55,'#0f172a');bg.addColorStop(1,'#111827');ctx.fillStyle=bg;ctx.fillRect(0,0,1080,1350);const glow=ctx.createRadialGradient(880,160,10,880,160,500);glow.addColorStop(0,'rgba(180,255,69,.22)');glow.addColorStop(1,'rgba(180,255,69,0)');ctx.fillStyle=glow;ctx.fillRect(0,0,1080,720);const box=(x:number,y:number,w:number,h:number)=>{ctx.beginPath();ctx.roundRect(x,y,w,h,28);ctx.fillStyle='rgba(255,255,255,.045)';ctx.fill();ctx.strokeStyle='rgba(255,255,255,.09)';ctx.stroke();};ctx.fillStyle='#b4ff45';ctx.font='700 28px Arial';ctx.fillText('KG STAY ACTIVE CHALLENGE',70,85);ctx.fillStyle='#94a3b8';ctx.font='600 22px Arial';ctx.fillText('SEASON COMPLETE',70,128);ctx.fillStyle='#f8fafc';ctx.font='900 62px Arial';ctx.fillText(data.challengeName.slice(0,28),70,220);ctx.fillStyle='#94a3b8';ctx.font='500 24px Arial';ctx.fillText(`${date(data.startDate)} – ${date(data.endDate)}`,70,265);box(70,330,940,250);ctx.fillStyle='#64748b';ctx.font='700 20px Arial';ctx.fillText('CHAMPION',105,385);ctx.fillStyle='#f8fafc';ctx.font='900 58px Arial';ctx.fillText(data.champion?.name??'No champion',105,470);ctx.fillStyle='#b4ff45';ctx.font='900 42px Arial';ctx.fillText(data.champion?`${data.champion.points.toFixed(1)} points`:'—',105,530);box(70,625,455,190);box(555,625,455,190);ctx.fillStyle='#64748b';ctx.font='700 18px Arial';ctx.fillText('WINNING COLUMN',105,675);ctx.fillText('SEASON TOTAL',590,675);ctx.fillStyle='#f8fafc';ctx.font='800 34px Arial';ctx.fillText((data.winningColumn?.name??'—').slice(0,20),105,735);ctx.fillText(`${data.totalPoints.toFixed(1)} pts`,590,735);ctx.fillStyle='#94a3b8';ctx.font='500 20px Arial';ctx.fillText(data.winningColumn?`${data.winningColumn.points.toFixed(1)} pts`:'',105,775);ctx.fillText(`${data.activityCount} activities · ${data.activeAthletes} athletes`,590,775);ctx.fillStyle='#fb923c';ctx.font='800 20px Arial';ctx.fillText('TOP THREE',70,885);data.topThree.forEach((row,index)=>{const y=940+index*78;ctx.fillStyle=index===0?'#b4ff45':'#f8fafc';ctx.font='800 28px Arial';ctx.fillText(`#${row.rank} ${row.name.slice(0,25)}`,90,y);ctx.textAlign='right';ctx.fillText(`${row.points.toFixed(1)} pts`,990,y);ctx.textAlign='left';});if(data.personal){box(70,1190,940,90);ctx.fillStyle='#94a3b8';ctx.font='700 18px Arial';ctx.fillText('YOUR SEASON',100,1230);ctx.fillStyle='#f8fafc';ctx.font='800 26px Arial';ctx.fillText(`#${data.personal.rank} · ${data.personal.points.toFixed(1)} pts · ${data.personal.distanceKm.toFixed(1)} km`,100,1265);}ctx.fillStyle='#64748b';ctx.font='600 18px Arial';ctx.fillText('kgstayactivechallenge.app',70,1320);return await new Promise<Blob>((resolve,reject)=>canvas.toBlob(blob=>blob?resolve(blob):reject(new Error('Export failed')),'image/png'));};
  const save=async()=>{try{setMessage('Preparing final card…');const blob=await buildImage();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`kg-${data.seasonKey}-season-final.png`;a.click();URL.revokeObjectURL(url);setMessage('Final season card saved.');}catch{setMessage('Could not create the final card on this device.');}};
  const share=async()=>{try{setMessage('Preparing final card…');const blob=await buildImage();const file=new File([blob],`kg-${data.seasonKey}-season-final.png`,{type:'image/png'});const text=`${data.challengeName} complete — Champion: ${data.champion?.name??'—'} · Winning column: ${data.winningColumn?.name??'—'}.`;if(navigator.share&&navigator.canShare?.({files:[file]})){await navigator.share({title:`${data.challengeName} final results`,text,files:[file]});setMessage('Final results shared.');return;}if(navigator.share){await navigator.share({title:`${data.challengeName} final results`,text,url:window.location.origin});setMessage('Final results shared.');return;}await navigator.clipboard.writeText(`${text} ${window.location.origin}/results`);setMessage('Final result summary copied.');}catch(error){if((error as Error)?.name!=='AbortError')setMessage('Sharing is unavailable on this device.');}};
  return <section className="space-y-5"><div className="overflow-hidden rounded-3xl border border-lime-300/20 bg-[radial-gradient(circle_at_top_right,_rgba(180,255,69,0.18),_transparent_38%),radial-gradient(circle_at_bottom_left,_rgba(251,146,60,0.12),_transparent_42%),#0f172a] p-6 shadow-2xl shadow-black/20 sm:p-8"><p className="flex items-center gap-2 text-xs font-black uppercase tracking-[0.2em] text-lime-300"><Sparkles className="h-4 w-4"/>Challenge complete</p><div className="mt-4 flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><h1 className="text-4xl font-black sm:text-6xl">Season final results</h1><p className="mt-3 text-slate-400">{data.challengeName} · {date(data.startDate)} – {date(data.endDate)}</p></div><div className="flex gap-2"><button type="button" onClick={save} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/10 px-4 py-2 text-sm font-bold text-slate-300"><Download className="h-4 w-4"/>Save final card</button><button type="button" onClick={share} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-lime-300 px-4 py-2 text-sm font-black text-slate-950"><Share2 className="h-4 w-4"/>Share results</button></div></div>{message?<p className="mt-3 text-xs text-slate-400">{message}</p>:null}<div className="mt-8 grid gap-4 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-3xl border border-yellow-300/20 bg-yellow-300/[0.06] p-6"><Crown className="h-8 w-8 text-yellow-300"/><p className="mt-4 text-xs font-black uppercase tracking-wider text-yellow-300">Season champion</p><Link href={data.champion?`/participants/${data.champion.userId}`:'#'} className="mt-2 block text-3xl font-black hover:text-yellow-200">{data.champion?.name??'No champion recorded'}</Link><p className="mt-2 text-xl font-black text-lime-300">{data.champion?`${data.champion.points.toFixed(1)} points`:'—'}</p><p className="mt-1 text-sm text-slate-500">{data.champion?.columnName}</p></div><div className="rounded-3xl border border-cyan-300/20 bg-cyan-300/[0.05] p-6"><Users className="h-8 w-8 text-cyan-300"/><p className="mt-4 text-xs font-black uppercase tracking-wider text-cyan-300">Winning column</p><p className="mt-2 text-3xl font-black">{data.winningColumn?.name??'No column result'}</p><p className="mt-2 text-xl font-black text-cyan-300">{data.winningColumn?`${data.winningColumn.points.toFixed(1)} points`:'—'}</p><p className="mt-1 text-sm text-slate-500">{data.winningColumn?`${data.winningColumn.activeAthletes} active athletes`:''}</p></div></div></div>
  <div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><h2 className="flex items-center gap-2 text-xl font-black"><Medal className="h-5 w-5 text-yellow-300"/>Final podium</h2><div className="mt-4 space-y-2">{data.topThree.map(row=><Link key={row.userId} href={`/participants/${row.userId}`} className="flex items-center gap-3 rounded-xl bg-black/15 p-4"><span className="w-9 text-xl font-black text-yellow-300">#{row.rank}</span><span className="min-w-0 flex-1"><strong className="block truncate">{row.name}</strong><span className="text-xs text-slate-500">{row.columnName}</span></span><span className="font-black text-lime-300">{row.points.toFixed(1)}</span></Link>)}</div></section><section className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><h2 className="flex items-center gap-2 text-xl font-black"><Trophy className="h-5 w-5 text-orange-300"/>Category winners</h2><div className="mt-4 grid gap-2 sm:grid-cols-2">{data.categoryWinners.map(winner=><Link key={winner.category} href={`/participants/${winner.userId}`} className="rounded-xl bg-black/15 p-4"><p className="text-[0.65rem] font-black uppercase tracking-wide text-orange-300">{winner.label}</p><p className="mt-1 truncate font-black">{winner.name}</p><p className="mt-1 text-sm text-slate-500">{winner.points.toFixed(1)} pts</p></Link>)}</div></section></div>
  {data.personal?<section className="rounded-2xl border border-lime-300/20 bg-lime-300/[0.05] p-5 sm:p-6"><p className="text-xs font-black uppercase tracking-[0.16em] text-lime-300">Your season</p><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-5"><Stat label="Final rank" value={`#${data.personal.rank}`}/><Stat label="Points" value={data.personal.points.toFixed(1)}/><Stat label="Activities" value={data.personal.activities.toString()}/><Stat label="Distance" value={`${data.personal.distanceKm.toFixed(1)} km`}/><Stat label="Buddy sessions" value={data.personal.buddySessions.toString()}/></div></section>:null}
  <section className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 sm:p-6"><h2 className="text-xl font-black">Season records</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{data.records.map(record=><div key={record.key} className="rounded-xl bg-black/15 p-4"><p className="text-[0.65rem] font-black uppercase tracking-wide text-slate-500">{record.label}</p><p className="mt-2 text-lg font-black text-yellow-200">{record.displayValue}</p><p className="mt-1 truncate text-sm text-slate-400">{record.holderName}</p></div>)}</div></section></section>;
}
function Stat({label,value}:{label:string;value:string}){return <div className="rounded-xl bg-black/15 p-4"><p className="text-[0.65rem] uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-xl font-black">{value}</p></div>;}function date(value:string){return new Date(value).toLocaleDateString('en-SG',{timeZone:'Asia/Singapore',day:'numeric',month:'short',year:'numeric'});}
